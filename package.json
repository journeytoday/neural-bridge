{
  "name": "neural-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local NeuralBridge reference application and intelligence SDK",
  "scripts": {"start": "node server.js", "test": "node --test", "check": "node --check src/app.js && node --check server.js"},
  "engines": {"node": ">=22"}
}
